# DG-029：平衡mode探针 + 结构标量route探针（零更新）

结论：**V3-003方向(a)获证据支持：注入3个结构标量后route探针r2 recall 0.875（阈值≥0.7）；
mode start在平衡采样下0.996可分——两个失败都是"信息在、读出没学到"，且都有可行修复路径。**
注册：`../../experiments/DG-029.json`；检查：`checks/DG-029-probe.json`。

## 结果

| 类 | 训练头 | 纯表征探针(DG-028) | +结构标量(标准化) |
| --- | ---: | ---: | ---: |
| r0 | 0.50 | 0.97 | **1.00** |
| r1 | 1.00 | 0.45 | **0.70** |
| r2 | 0.18 | 0.45 | **0.875** |

结构标量：has_source、span数、allowed占比（3维）。第一轮未标准化直接拼接时探针反而退化
（r0 0.97→0.5），判定为标量尺度+LBFGS优化伪影；标准化后复测得到上表。两次运行均保留。

mode（2180负例:268正例，修复DG-028采样缺陷）：generate 0.999 / start 0.996——完全可分，
DG-028的"1.0/近单类"缺陷结论作废，本结果为有效证据；但探针可能利用位置规律，语义泛化未证。

## V3-003提案依据

(a) route头输入扩展3结构标量（新子类模块实现，不改被pin的FineSpanReader——可选参数默认关闭
保持旧路径参数集与摘要不变）；
(b) mode头输入detach训练（stop-grad，读出层在可分输入上独立收敛，不被干路拖走）。
同数据/种子/50步、EV-003同面板同门槛。**等待用户预算批准。**

复现：

```sh
python3 python/check_structural_probe.py
```
