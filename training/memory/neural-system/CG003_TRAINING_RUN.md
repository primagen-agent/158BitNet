# CG-003：已批准的两组50步训练

2026-09-19，用户明确批准“通过前置检查后，两组各50步，总计100步，不增加种子或自动延长”。
预算记录为 `experiments/CG-003-budget-approval.json`。

## 已完成

- 固定24例无记忆基线完成独立双评审，24/24标注一致，无待裁决项；详见 `reviews/CG-003/BASELINE_PROCESS.md`。
- 本地回归251项Python测试、21项CTest通过；单元测试不作为记忆能力证据。
- 完整训练快照476个源码文件、164个特征文件，在新GPU目录逐文件校验；没有覆盖旧训练目录。
- 真实首批4对数据走实际优化循环，在更新之前拦截；两种策略CPU/CUDA裁剪后累积梯度通过 `atol=1e-4, rtol=1e-3`。
  最大绝对误差分别为 `2.30446e-5`、`7.39843e-6`，真实模型在这项预检中没有更新。
- 人工8维/13词表夹具在CPU/CUDA各跑50步，检查点往返一致；最终权重最大误差分别为 `1.40071e-6`、`8.34465e-7`。
  夹具只是优化器与文件校验，不是额外记忆候选。
- 前置证据通过后，RTX 5070上 `joint_aux` 与 `joint_product` 均完成50步；总计100步并停止。
  每组使用同一200对/400次记录使用的固定表，不恢复旧模型，不更新GGUF，不使用LoRA/SVD/NAS或LoCoMo。

GPU预检报告：`checks/CG-003-optimizer-preflight.json`，SHA `ef9b846676b743da8135d681048b1974bf11d3bff3017209d2a5ad01cce4216b`。
源码/特征归档SHA：`1f4dd60641809dd59f8600f25961b0c53e79ef60cd011a687473690489495e6b`。
服务器目录：`/home/ubuntu/158bitnet-locomo/build/cg003-training-1f4dd606/`。
骨干仍为SHA `44cb4e0db8374d4247bba391b3d3b7c0b3ee815c36cf2e20d0d1a3570677bd95` 的同一0.5B GGUF。

## 训练产物与诊断

| 策略 | 正式步数 | dev状态分类正确数 | 第50步检查点SHA |
| --- | ---: | ---: | --- |
| joint_aux | 50 | 165/192 | `fb4728d552c63d4ca6881b7141766240a1df3abf3c73e7b92a40d69475d3dc36` |
| joint_product | 50 | 170/192 | `d40a254c82aa1602b5e1c2bfa63dc971f7079666539bf1fb9550e3a43f7cff8a` |

两个策略第0步参数摘要相同：`b738c27ab6f6e2b1119a90d825ac106d7556ef4ce2491e1dd42c8fbcfbd13a74`。
表中只是三状态分类，不是城市回答正确率，更不是LoCoMo记忆准确率；不能按这个数字选优。
训练日志每一步包含4对实际记录ID、损失及裁剪前梯度范数。首末步来自不同批次，不能将其差直接解释为统一测试集改善。
正式模型是否能recall，仍须看固定24例的真实C自由回复及独立评分。

服务器结果包SHA：`9ef6e9c0cac7440aef4e8f25e3ace05a7d1d11454b442846e8d982e54bb9b4b4`。
结果包包含两组第0/50步检查点、完整训练日志、dev诊断、状态和启动证据，不仅保存更好的一组。
这些是Python研究检查点，不是已经部署的C记忆模型。

完整结果包已下载到 `build/neural-memory-cg003-training-results.tar.gz` 并核验SHA；
展开目录为 `build/neural-memory-cg003-training-results/`，4个检查点分别与服务器状态清单一致。
状态与dev诊断副本见 `checks/CG-003-joint_aux-{status,dev}.json` 和 `checks/CG-003-joint_product-{status,dev}.json`。

需要保留的风险：两组normal状态均32/32正确，但aux的supported仅44/64、insufficient89/96；
product的supported64/64、insufficient74/96。**role-swap状态识别分别为9/16和0/16**。
因此较高的总状态分类数掩盖了乘法组的联合绑定问题，不能据170/192把它选为更好的记忆模型。
这是训练后的诊断发现，不据此更改固定C面板或追加训练。

## 本地加载与C测试状态

Mac使用Torch2.12.1，GPU使用2.13.0+cu130；重建第0步模型时，仅 `reader.slot_queries` 的816/2048个元素
发生最大 `4.470348358154297e-8` 的差异，其余参数逐位相同。根本原因尚未区分Torch版本与CPU归一化实现，
但已确定不是下载损坏：原始第0步文件和张量摘要均匹配，两个训练策略的初始张量相同。
因此不能要求跨平台重新随机初始化的浮点归一化结果逐位相同。

新增 `joint_checkpoint_transport.py` 先验证独立绑定的第0步文件SHA、类型、参数清单、空优化器状态与张量摘要，
恢复训练机的精确初始字节，再让原严格加载器检查第50步模型及冻结参数。不放宽容差，不修改训练权重，
不拿最终参数充当初始化；错误或伪装的初始文件仍拒绝。2项专项回归通过。
训练源码没有修改，新增的加载适配器和C评估脚本单独版本化。追加验证后Python253/253通过；C源码未改，CTest21/21记录仍有效。

两组检查点均已通过本地加载，固定24例C自由生成与独立双评审现已完成，见 `reviews/CG-003/PROCESS.md`。
两组有依据回答均1/8、换值成对均0/4，分别截断1例和6例，均未通过门槛；不选优、不部署、不追加训练。
结果目录分别为 `build/neural-memory-cg003-joint_aux-generation/` 与 `build/neural-memory-cg003-joint_product-generation/`。

## 启动与剩余边界

`run_joint_training.py`/`joint_launch.py` 验证明确批准、模型与完整特征身份、冻结基线与双评审、
GPU优化器预检及不可变源码快照。JSON状态字段本身不能放行。
每组在 `/home/ubuntu/158bitnet-locomo/build/cg003-approved-20260919/` 原子创建一次预算领取记录，
即使失败也不自动重跑；两组预算现在均已消耗。保持无resume、无自动续训、无部署批准。

已执行快照中的 `prerequisites_complete=false` 是打包时的准备状态；启动时由实际预检报告逐项验证，
没有修改已封存源码或靠翻转该字段绕过检查。当前登记文件已更新为训练完成并记录实际启动命令。
训练后添加的本地评估脚本/报告不属于该476文件的执行快照；模型及损失实现仍使用已验证版本。

检查点取回、身份核验、两组固定24例真实C前缀/预测路线生成及双评审均已完成。
两组均失败，已按登记停止，不加步数、不改种子、不用LoCoMo选择；后续先登记零更新诊断而不是延长训练。
